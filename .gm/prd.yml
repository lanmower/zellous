- id: fix-vad-listen-track-born-muted
  subject: 'User live-diagnosed VAD auto-transmit deadlock: entering VAD mode calls setMuted(true), which disables the raw mic track; the VAD level meter and speaker detector read RMS off a supposedly-independent cloned track (_localListenTrack, connect() lines 251-260, documented as existing specifically to stay always-enabled for local analysis regardless of mute state). Root-caused precisely via a real live browser test (cdp dispatch against a real getUserMedia stream): MediaStreamTrack.clone() inherits the CURRENT enabled state of the source track at the moment of cloning -- it does NOT default to true. connect()''s clone happens at line 258, AFTER line 248 already set the original track''s enabled to !pttMode (false in the default PTT-starts-muted case) -- so _localListenTrack is born already disabled. setMuted() (lines 311-320) never touches _localListenTrack afterward, only this.localStream''s original tracks -- so the clone stays permanently disabled for the entire session, defeating its own documented purpose from the very first connect. This produces exactly the symptom the user observed: rms/level pinned at 0 the whole time VAD is idle, and VAD auto-transmit can never trigger since speech can never be detected on a permanently-silenced analyser input. Postcondition: _localListenTrack must always be genuinely enabled regardless of the original track''s mute state or clone-order timing, live-verified via a real getUserMedia stream + real AnalyserNode reading non-zero RMS while the session is muted.'
  SESSION_ID: zellous-turnfix-1787667820
  status: pending
