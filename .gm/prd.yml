- id: wire-vad-threshold-and-fix-stale-vendor
  subject: 'User grepped docs/vendor/wireweave/src/voice.js and found local-level missing -- root-caused to that file being a STALE committed copy from before this session (last touched by commit 78edd5a, predates the local-level fix), while the real submodule source (wireweave/src/voice.js) and the deployed production site both already have it (live-verified earlier this session). CI regenerates docs/vendor/wireweave/src at build/deploy time but never commits the regenerated copy back, so the repo''s own tracked source permanently drifts behind what actually ships -- a real bug (misleading source-of-truth), separate from the VAD fix itself, needs fixing (refresh the committed copy to match, or stop committing a build artifact at all). User''s second finding is real and unfixed: nostr-adapter.js''s voiceSettingsChange only writes S.vadThreshold.value on a threshold change, never calls into the real voice session (window.lk has no setMicSensitivity exposed at all in wireweave-bridge.js''s voiceAPI facade, and wireweave.js''s real setMicSensitivity(rms) method is never called from anywhere in zellous). The VAD threshold slider is fully cosmetic -- it changes UI state but never actually retunes speaker-activity detection, and the persisted value is never reapplied at connect() start either. User''s third question (why VAD-transmit doesn''t fire) is not an independent bug -- traced the full chain (_pollActivity -> _setSpeaking -> emit(''speaker'') -> voice-ptt.js''s onVoiceConnected wiring window.lk.on(''speaker'', onLocalSpeaker)) and confirmed it is structurally intact and was live-witnessed firing correctly earlier this session (a synthetic loud signal correctly triggered speaking:true). The real remaining question is calibration -- whether real speech crosses the hardcoded 0.045 default RMS threshold in practice, which fixing the threshold-wiring gap directly addresses (the user could not previously retune it at all).'
  SESSION_ID: zellous-turnfix-1787667820
  status: pending
