- id: add-design-wireweave-submodules
  subject: 'Add git submodules design/ (https://github.com/AnEntrypoint/design.git) and wireweave/ (https://github.com/AnEntrypoint/wireweave.git) at repo root, tracking each repo''s main branch, for local editing access. Precondition: no existing .gitmodules (confirmed absent). Postcondition: .gitmodules exists with both entries, git submodule status shows both checked out at main''s current tip, README/AGENTS.md documents the local-edit workflow (git submodule update --remote --merge to refresh).'
  SESSION_ID: zellous-design-1787650200
  status: completed
  witness: 'Committed 21e8ab2: .gitmodules with both entries, design/ and wireweave/ checked out clean at each repo''s main tip (e91d786, d2a400e). Pushed; both CI and Deploy GH Pages green.'
  commit_comment: Submodules added for local editing per user direction
- id: ci-build-from-submodules
  subject: 'Wire gh-pages.yml (and ci.yml where relevant) to: (1) checkout with submodules:true and update each submodule to its remote main tip before build (git submodule update --remote --merge, committed back or done transiently in CI -- decide which at EXECUTE), (2) run npm ci + npm run build inside design/ to produce design/dist/247420.{js,css}, (3) copy design/dist/247420.{js,css} and wireweave/src/** into the paths docs/nostr-chat/index.html''s importmap+stylesheet reference so flatspace''s existing docs-asset-copy ships them, (4) remove the jsdelivr CDN dependency from the importmap injector and stylesheet link entirely. Precondition: add-design-wireweave-submodules resolved first (submodules must exist before CI can build from them). Postcondition: a fresh push with no jsdelivr network calls in the browser-witness step (verify via network trace or absence of cdn.jsdelivr.net in CI logs/requests), app boots identically (appReady, zero console errors), CI green.'
  SESSION_ID: zellous-design-1787650200
  status: pending
- id: remove-status-footer-bar
  subject: 'Remove the entire .app-status footer bar (''Zellous Public''/server name, ''nothing here yet''/message count, room count, ''powered by anentrypoint-design'' link): delete the wireStatusBar IIFE in docs/js/ui-shell.js, the <footer id="zStatus"> markup in docs/nostr-chat/index.html, and its CSS block in docs/css/zellous.css (the .app-status rules incl. position:fixed/height:42px and the .chat/.ca-app calc() that reserved 42px for it). Precondition: footer currently renders (confirmed live: zStatusServer/zStatusMsgs/zStatusRooms + powered-by link all present). Postcondition: footer DOM/CSS/JS fully gone, no dead space at the bottom where it was, .chat and voice view fill the freed vertical space, zero console errors, live-witnessed via browser.'
  SESSION_ID: zellous-design-1787650200
  status: pending
- id: fix-mobile-vc-button-safe-area
  subject: 'Fix mobile/tablet bottom-area positioning so voice-control (VC) buttons (mic/mute/deafen/settings in the SDK''s .vx-view voice surface) appear correctly above the safe-area/home-indicator, not misplaced -- currently zero .vx-*/.vx-view CSS overrides exist in zellous.css (confirmed via codesearch) so the voice view has no safe-area-bottom accommodation of its own; the only existing mobile safe-area fix (commit 2302e680) is a coarse html,body env(safe-area-inset-*) padding that does not specifically address voice-control positioning, and removing the .app-status footer bar (remove-status-footer-bar) changes the available bottom space calculus this depends on -- must be done AFTER that row resolves, re-measure fresh. Precondition: remove-status-footer-bar resolved (footer''s freed space and any lingering height-calc assumptions are gone first). Postcondition: live-witnessed at 375x667 and 768x1024 (phone + tablet viewports) via browser verb -- VC buttons fully visible and tappable above any safe-area inset / home indicator, zero overlap with system chrome, zero console errors.'
  SESSION_ID: zellous-design-1787650200
  status: pending
- id: swap-marketing-landing-cdn-to-submodule
  subject: 'docs/index.html (live marketing landing) links jsdelivr CSS directly, and site/theme.mjs (flatspace-build-time) both Node-imports ''anentrypoint-design'' via package.json''s npm dep AND injects its own browser importmap pointing SDK_URL at the same jsdelivr URL for the built landing page. Once design/ is a submodule and CI builds from it (ci-build-from-submodules), these three references (docs/index.html link, theme.mjs''s SDK_URL const, theme.mjs''s Node-side npm import) should point at the submodule-built output too, for consistency -- no CDN dependency left anywhere in the deployed surface. Decide at EXECUTE whether theme.mjs''s Node-import switches from the npm package to a relative path into design/ (submodule), or keeps npm for build-time convenience while only the runtime browser importmap/CSS link changes -- name the decision in the commit. Precondition: ci-build-from-submodules resolved (the build artifact path must exist first). Postcondition: zero cdn.jsdelivr.net references remain in docs/index.html, site/theme.mjs, or docs/nostr-chat/index.html; flatspace build succeeds; landing page live-witnessed with correct styling.'
  SESSION_ID: zellous-design-1787650200
  status: pending
