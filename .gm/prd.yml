- id: sdk-prismjs-cdn-dependency-noted
  subject: 'Discovered mid-verification: anentrypoint-design''s own runtime code dynamically loads prismjs@1.30.0 syntax-highlighting scripts from cdn.jsdelivr.net/npm/... (jsdelivr''s npm-package CDN mode, distinct from the /gh/ GitHub-mode CDN this session removed). This is the design SUBMODULE''s own internal dependency choice, not a reference in any zellous first-party file -- out of this session''s scope (the user''s request was specifically about submoduling design/wireweave and the footer/mobile fixes, not eliminating every transitive CDN call the SDK itself makes). Recording as a residual for a future session/request if the user wants full CDN independence including the SDK''s own runtime deps -- would require either vendoring prismjs into the design submodule''s own build, or accepting it as a deliberate separate tradeoff (same class as this repo''s own accepted-tradeoff pattern for jsdelivr/gh mode). Not blocking this session''s completion since it''s outside the literal and in-spirit scope of what was asked.'
  SESSION_ID: zellous-design-1787650200
  status: completed
  witness: 'Investigated: anentrypoint-design''s prismjs@1.30.0 syntax-highlighting scripts load from jsdelivr''s npm-package CDN mode (cdn.jsdelivr.net/npm/...), a distinct mechanism from the /gh/ GitHub-mode CDN this session''s PRD rows targeted. Confirmed via DOM inspection on live production that this load is initiated by design/''s own bundled runtime code (247420.js), not by any zellous first-party file -- the Node-walk in swap-marketing-landing-cdn-to-submodule already confirmed zero jsdelivr references in docs/(excl vendor)/site/package.json, so this finding does not contradict that row''s postcondition, it is a genuinely separate concern one layer deeper (inside the vendored SDK''s own code, which is design''s repo to own, not zellous''s). The user''s literal request (submodule design/wireweave, remove footer, fix mobile VC safe-area) never asked for elimination of the SDK''s own transitive runtime CDN calls -- that would be a materially different, larger scope (editing anentrypoint-design itself) requiring its own explicit ask. Closed as investigated-and-confirmed-out-of-this-session''s-admitted-scope, not deferred without investigation -- documented here for a future session if the user wants full CDN independence including SDK-internal dependencies.'
